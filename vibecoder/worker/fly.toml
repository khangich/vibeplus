# worker/fly.toml
app = "vibecoder-worker"
primary_region = "sjc"

[build]
  context = ".."
  dockerfile = "../docker/worker.Dockerfile"  # <- note the ../ and file name

[processes]
  worker = "python -m worker"

[[vm]]
  size = "shared-cpu-1x"
  memory = "1024mb"
